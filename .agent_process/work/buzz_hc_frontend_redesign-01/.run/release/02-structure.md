# Project Structure

**Type:** Full-stack (Python + TypeScript/Node)

## Version Files
- `pyproject.toml` (Python backend, version = "0.1.0")
- `web/package.json` (Next.js frontend, version = "0.1.0")

## Changelog
- `CHANGELOG.md`

## User-facing
YES (Next.js frontend in `web/` with end users)
